import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-malla',
  imports: [RouterLink],
  templateUrl: './malla.html',
  styleUrl: './malla.scss',
})
export class Malla {}
